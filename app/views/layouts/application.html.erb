<!DOCTYPE html>
<html>
  <head>
    <title>MeuCapilar</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if notice %>
      <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-4 fixed right-0 top-0 m-4 z-50 shadow-lg">
        <p><%= notice %></p>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4 fixed right-0 top-0 m-4 z-50 shadow-lg">
        <p><%= alert %></p>
      </div>
    <% end %>
    
    <main>
      <%= yield %>
    </main>
  </body>
</html>
